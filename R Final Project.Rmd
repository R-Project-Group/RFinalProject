---
title: "Final Project"
author: "Phillip Ross"
date: "5/6/2021"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
if(!require('pacman')) {install.packages('pacman')}
pacman::p_load(tidyverse, dplyr, ggplot2, ggthemes, data.table, lubridate,
               GGally, RColorBrewer, ggsci, plotROC, usmap,
               plotly, ggpubr, vistime)
```

## R Markdown

This is an R Markdown document. Markdown is a simple formatting syntax for authoring HTML, PDF, and MS Word documents. For more details on using R Markdown see <http://rmarkdown.rstudio.com>.

When you click the **Knit** button a document will be generated that includes both content as well as the output of any embedded R code chunks within the document. You can embed an R code chunk like this:

```{r}
killdata <- read.csv("PoliceKillingsUS.csv")
incomedata <- read.csv("MedianHouseholdIncome2015.csv")
povertydata <- read.csv("PercentagePeopleBelowPovertyLevel.csv")
educationdata <- read.csv("PercentOver25CompletedHighSchool.csv")
racedata <- read.csv("ShareRaceByCity.csv")
popdata <- read.csv("PopulationData.csv")
```


```{r}
killdata$location <- paste(killdata$city, killdata$state, sep="")
head(killdata)
```

Note that the `echo = FALSE` parameter was added to the code chunk to prevent printing of the R code that generated the plot.
